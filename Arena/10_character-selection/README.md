# Character Selection

This lane defines the design intent for arena avatar selection.

The focus is temporary arena state, not permanent-character progression. Selection should fit the portable arena rules framework and remain reusable across arena region instances.